{
  "name": "product-price-tracker-monorepo",
  "version": "1.0.0",
  "description": "Product Price Tracker - Full Stack Web Scraping Solution for INE Mock Store",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build:all": "npm run build --prefix backend && npm run build --prefix frontend",
    "test": "npm test --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "scraper:headed": "npm run scraper:headed --prefix backend",
    "start:backend": "npm start --prefix backend"
  }
}
